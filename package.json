{
    "name": "@cap-js/attachments",
    "description": "CAP cds-plugin providing image and attachment storing out-of-the-box.",
    "version": "1.0.0",
    "main": "cds-plugin.js",
    "files": [
        "index.cds",
        "lib",
        "srv"
    ],
    "devDependencies": {
        "@cap-js/sqlite": "^1.0.1",
        "axios": "^1.4.0",
        "eslint": "^8",
        "express": "^4.18.2",
        "jest": "^29.5.0"
    },
    "peerDependencies": {
        "@sap/cds": "^7"
    },
    "cds": {
        "requires": {
            "attachments": {
                "model": "@cap-js/attachments"
            }
        }
    },
    "scripts": {
        "lint": "npx eslint .",
        "test": "npx jest --silent"
    },
    "dependencies": {
        "@aws-sdk/client-s3": "^3.400.0"
    }
}
